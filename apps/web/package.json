{
  "name": "@authority/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "npm run build -w @authority/shared",
    "dev": "next dev -p ${WEB_PORT:-3000}",
    "prebuild": "npm run build -w @authority/shared",
    "build": "next build",
    "start": "next start -p ${WEB_PORT:-3000}",
    "test": "echo \"No tests in web yet\"",
    "pretypecheck": "npm run build -w @authority/shared",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@authority/shared": "*",
    "next": "^15.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.16",
    "typescript": "^5.6.3"
  }
}
